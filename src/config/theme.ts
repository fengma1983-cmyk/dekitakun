// =====================================================================
// src/config/theme.ts
// =====================================================================
// 色・タイポグラフィ・spacing など視覚定数の単一出典。
//
// なぜここに集約するか:
//   Architecture 8.4「色を変更する → theme.ts のみ編集」。
//   カラーコードを JSX や CSS の中に直接書くことは禁止。
//   ハードコードが散ると、息子の「この色いや」に迅速対応できない。
// =====================================================================

// ---------------------------------------------------------------------
// カテゴリ別カラー (柔らかいパステル基調)
// なぜパステルか: 11 歳向け。原色の強いコントラストは視覚的疲労を招く。
// なぜ homework だけ saturated な緑 (#51CF66) か:
//   息子が v1.0 プロトタイプ (dekita-kun.html) で最も多く触れた色。
//   視覚的連続性を 1 点保持するため、v1.1 でも同じ色で残す。
//   初回 reveal の反応を観察し、違和感があった場合は fake-a-day
//   レポートに記録した上で、他カテゴリの saturated 復元を再検討する。
// ---------------------------------------------------------------------
export const CATEGORY_COLORS = {
  school:   "#A7C7E7",  // 水色
  homework: "#51CF66",  // 緑 (prototype 継承 / 上記コメント参照)
  cram:     "#CBAACB",  // 藤色
  study:    "#B5EAD7",  // ミント
  play:     "#FFB7B2",  // サーモン
  meal:     "#FFDAC1",  // 薄橙
  bath:     "#C7CEEA",  // ラベンダー
  sleep:    "#B5B5B5",  // グレー
} as const;

// ---------------------------------------------------------------------
// 難易度表示色
// ---------------------------------------------------------------------
export const DIFFICULTY_COLORS = {
  easy:      "#B5EAD7",
  normal:    "#A7C7E7",
  challenge: "#FFB7B2",
} as const;

// ---------------------------------------------------------------------
// 状態表示色
// なぜ carried / paused / postponed を別の色にするか:
//   この 3 つの状態は意味が異なる (types.ts 参照)。視覚でも区別する。
//   ただし赤系や警告色は使わない。いずれも柔らかい色で「責めない」空気。
// ---------------------------------------------------------------------
export const STATUS_COLORS = {
  planned:    "#E8E8E8",  // 薄グレー (まだ始まっていない)
  inProgress: "#FFE66D",  // 黄 (now 状態を柔らかくハイライト)
  paused:     "#D5C8E8",  // 柔らかい藤色。opacity を下げる演出と併用
  postponed:  "#C9D9E8",  // 柔らかい水色 (別時刻に移動した感)
  done:       "#B5EAD7",  // ミント (達成)
  carried:    "#E8E0D0",  // 柔らかいベージュ (「待続」の空気感)
} as const;

// ---------------------------------------------------------------------
// ブランドカラー
// ---------------------------------------------------------------------
// PURPLE は dekita-kun.html (v1.0 プロトタイプ) 由来のアクセント。
// 息子が card session で触った色なので、視覚的連続性のため継承する。
// 用途: NowIndicator (いまピン)・primary action ボタン (できた！/ほぞん)。
// v1.1 では「赤系 = 警告」を使わないので、強い動作表現は紫に集約する。
export const PURPLE = {
  main:  "#6C63FF",   // 基本
  light: "#EDE9FF",   // 面で使う
  mid:   "#A89BFF",   // サブ
} as const;

export const BRAND = {
  primary:    "#FFC857",  // 太陽の黄 (温かさの色)
  accent:     "#2EC4B6",  // ターコイズ
  action:     PURPLE.main, // primary action (保存・できた！) 用
  background: "#FFFBF0",  // クリーム
  textMain:   "#3A3A3A",
  textSub:    "#7A7A7A",
} as const;

// ---------------------------------------------------------------------
// タイポグラフィ
// なぜ fontSizeBase が 18px か:
//   11 歳向け。モバイル標準 14〜16px より一段大きくして「読みやすい」を優先。
// ---------------------------------------------------------------------
export const TYPOGRAPHY = {
  fontFamily: "'Hiragino Maru Gothic Pro', 'Yu Gothic', sans-serif",
  fontSizeBase:  "18px",
  fontSizeLarge: "24px",
  fontSizeSmall: "14px",
  lineHeight: 1.6,
} as const;

// ---------------------------------------------------------------------
// Spacing / Radius
// ---------------------------------------------------------------------
export const SPACING = {
  xs: "4px",
  sm: "8px",
  md: "16px",
  lg: "24px",
  xl: "32px",
} as const;

export const RADIUS = {
  sm:   "6px",
  md:   "12px",
  lg:   "20px",
  pill: "999px",
} as const;
